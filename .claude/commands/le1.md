- 한국어로 답변해줘
- 현재 작업 또는 응답에서 파레토 법칙의 20%에 해당하는 내용을 먼저 눈에 띄게 표시하여 제시해줘
- 사용자 요청에서 헷갈리거나 확실히 이해되지 않는 부분이 있다면 내용을 정확하게 파악하기 위해 반드시 질문해줘
- 무조건 긍정하고 순응하지 말고 냉철하게 조언해줘
- 실제 구현 전에 반드시 구현 계획을 먼저 제시해줘
    - 사용자 요청에 포함되지 않은 부분도 포함시키려 할 경우 이 부분을 사용자가 분명하게 인지하게 해줘
- claude code 기본 도구를 mcp보다 우선 사용해줘
- 오늘 날짜 폴더(yy-mm-dd)를 작업 폴더로 사용해줘

# Coding
## 계획
- 내가 요청하지 않은 내용 제안 시 분명하게 추가 제안 내용 표시해줘
- 구현 진행 전에 내게 최종 동의 여부 묻고, 내 분명한 동의를 확인한 후에만 실제 구현 진행해줘

## 구현
- 내가 요청한 내용만 구현하고, 첨부 파일 참조 시에는 꼭 해당되는 내용만 참고해줘

## 파일 작업 
- 모든 코드 파일의 맨 위에는 아래 형식으로 '목차'를 작성한다. 단순 데이터 저장 파일에는 목차를 작성하지 않음. 
- 목차 생성의 목적은 개발자가 해당 파일에 대한 핵심 정보를 빠르고 효과적으로 파악하는 데 도움을 주기 위함
- 생성된 코드 파일은 수정되거나 삭제되지 않고, 수정 시에는 새로운 파일(_v2 같은 접미사 이용해 연속성 부여)을 만들고, 삭제 시에는 아래 상태 필드에 표시를 남긴다(soft delete)
- 주소는 제텔카스텐 시스템의 주소를 의미. 현재 파일의 이름을 주소로 입력하고, 그 이후 / 기호를 사용해 확장. 예시, demo-file/edited-v1 
- 목차 형식
    - 생성 시간: date 명령어를 실힝해 파일 생성 시의 한국 시간을 확인 후 기록 
    - 핵심 내용: 파일 핵심 내용에 대한 설명
    - 상세 내용: 
        - 파일 구성 요소의 이름(코드 라인 정보, 삽입된 주석 등의 정보도 고려된 정확한 정보): 해당 요소의 기능 또는 역할에 대한 간략한 설명
    - 상태: 
    - 주소: 
    - 참조: 파일 수정 시 이 파일이 수정한 원본 파일의 주소를 명시