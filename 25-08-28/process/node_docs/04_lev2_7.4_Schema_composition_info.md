# 속성
---
process_status: true
source: 2022_Data-Oriented Programming_Manning.pdf
source_type: book
source_language: english
structure_type: component
content_processing: unified
title: Data-Oriented Programming
folder_name: 
created_at: 2025-08-28T11:43:31.980838

# 추출
---
## 핵심 내용
Schema Composition은 JSON Schema를 조합하여 복잡한 데이터 유효성 검사를 수행하는 고급 기술입니다. `allOf` (AND), `anyOf` (OR), `oneOf` 키워드를 사용하여 여러 스키마를 결합하여 필드의 필수 여부 등 다양한 조건을 표현할 수 있습니다. 이는 클래스를 사용하는 것보다 유연하고 강력한 데이터 유효성 검사를 가능하게 하며, 부모 노드에서 제시한 데이터 검증의 유연성을 확장하는 데 기여합니다.

## 상세 핵심 내용
## 7.4 Schema Composition: 복잡한 조건, 강력한 유효성 검사

### 핵심 내용 및 배경: 외부 API 데이터 검증의 필요성
*   **핵심:** JSON Schema 조합 (Composition)을 통한 복잡한 데이터 구조 및 조건 표현 (부모 노드 핵심 내용 반영).
*   **배경 (재강조):** 외부 API 데이터 검증 필요성 (7.1절 연계, 시스템 경계 강조).
*   **예시:** Open Library Books API, "Watchmen" 책 정보 검증 (부모 노드 예시 재확인).

### 초기 스키마의 문제점 및 JSON Schema Composition 도입 (7.3절 연결)
*   **문제점 (재확인):** `isbn_10`, `isbn_13` 필수 여부 처리 (부모 노드 예시 재확인).
*   **JSON Schema Composition 소개 (7.2절 연계):** `allOf`, `anyOf`, `oneOf` (부모 노드 예시 재확인).

### JSON Schema Composition 구현 (예시 재강조)
1.  `basicBookInfoSchema` (제목 필수, 기타 필드 정의)
2.  `mandatoryIsbn13` (`isbn_13` 필수)
3.  `mandatoryIsbn10` (`isbn_10` 필수)
4.  `bookInfoSchema`: `basicBookInfoSchema` AND (`mandatoryIsbn13` OR `mandatoryIsbn10`) (부모 노드 예시 재확인).
*   **결과 (재강조):** 복잡한 필수 필드 조건 명확하게 표현 가능.

### JSON Schema Composition 사용의 장점 (7.2, 7.3절 연계)
*   **유연성 (재확인):** 여러 스키마 조합 (부모 노드 재확인).
*   **표현력 (재확인):** 클래스 기반보다 강력 (부모 노드 재확인).
*   **확장성 (재확인):** 다양한 조건 지원 (부모 노드 재확인).

### 추가 고려 사항 및 오류 정보 (7.5절 연결)
*   **검증 실패 시 오류 정보 (강조):** 상세 오류 정보 제공의 중요성 (7.5절 내용 언급).
*   **고급 데이터 검증 (언급):** Chapter 12에서 더 발전된 기법 다룸.

## 상세 정보
7.4 Schema composition: **7.4 스키마 조합을 통한 유효성 검사**

이 챕터는 스키마 조합(Schema Composition) 기술을 소개하며, 외부 API 응답 데이터의 유효성을 검사하는 데 초점을 맞춥니다. 부모 노드의 '7 Basic data validation' 내용과 연결하여, JSON Schema의 강력한 기능과 외부 데이터 검증의 중요성을 강조합니다.

**1. 문제 제기 (부모 노드 내용 반영):**

*   **외부 API 데이터의 유효성 검사 필요성:** 외부 웹 서비스 (예: Open Library Books API)로부터 수신된 데이터의 유효성을 검사해야 하는 상황을 제시하며, 데이터의 무결성 보장, 애플리케이션 안정성 향상, 보안 강화에 기여합니다. (부모 노드 '경계에서의 검증 예시' 참고)
*   **`isbn_10` 및 `isbn_13` 필드의 특수성:** `isbn_10`과 `isbn_13` 필드가 선택적이며, 둘 중 하나 또는 둘 다 존재할 수 있다는 점을 지적합니다. 단순한 스키마로는 이러한 조건을 표현하기 어렵습니다.

**2. Schema Composition 소개 (부모 노드 내용 반영):**

*   **개념:** 스키마 조합은 여러 스키마를 조합하여 복잡한 데이터 구조의 유효성을 검사하는 기법입니다. `allOf`, `anyOf`, `oneOf` 와 같은 키워드를 사용하여 스키마를 결합합니다. (부모 노드 'Schema composition' 참고)
*   **`allOf`, `anyOf`, `oneOf` 키워드:**
    *   `allOf`: 모든 스키마를 만족해야 합니다 (AND).
    *   `anyOf`: 최소한 하나의 스키마를 만족해야 합니다 (OR).
    *   `oneOf`: 정확히 하나의 스키마를 만족해야 합니다.
*   **구체적인 예시: Open Library Books API 응답 스키마 구성:**
    *   `basicBookInfoSchema`: 제목(`title`)만 필수적으로 요구하는 기본 정보 스키마.
    *   `mandatoryIsbn13`: `isbn_13`만 필수적으로 요구하는 스키마.
    *   `mandatoryIsbn10`: `isbn_10`만 필수적으로 요구하는 스키마.
    *   `bookInfoSchema`: `basicBookInfoSchema`를 만족하고, `mandatoryIsbn13` 또는 `mandatoryIsbn10` 중 하나 이상을 만족해야 합니다. 즉, 제목은 반드시 있어야 하고, `isbn_10` 또는 `isbn_13`은 최소한 하나는 있어야 합니다.

**3. 구현 예시 (Listing 7.18 & 7.19, 부모 노드 내용 활용):**

*   **스키마 정의 (Listing 7.18):** `basicBookInfoSchema`, `mandatoryIsbn13`, `mandatoryIsbn10` 및 `bookInfoSchema`의 JSON Schema 정의를 보여줍니다.
*   **데이터 검증 (Listing 7.19):** 실제 API 응답 데이터 (`bookInfo`)를 `bookInfoSchema`로 검증하는 예시를 제시합니다. 이 예시는 Watchmen 책에 대한 데이터를 사용하며, `isbn_10`과 `isbn_13` 모두를 포함하고 있습니다.

**4. 추가 정보 및 개선점 (부모 노드 내용 및 추가 보충):**

*   **스키마 구성의 장점:** 복잡한 데이터 유효성 검사 규칙을 표현하는 데 강력한 기능을 제공하며, 클래스를 사용하여 데이터를 표현하는 것보다 더 유연합니다. (부모 노드 '스키마 구성의 장점' 참고)
*   **유효성 검사 오류 메시지의 중요성:** 오류 메시지가 불명확할 경우 문제 해결이 어려우므로, 명확한 오류 메시지를 제공해야 합니다. (7.5절에서 자세히 다룸, 부모 노드 '데이터 검증 실패 정보' 참고)
*   **고급 데이터 유효성 검사:** 12장에서 다룰 고급 데이터 유효성 검사 기술에 대한 언급을 통해, JSON Schema의 더 강력한 기능을 기대하게 합니다.
*   **유연한 스키마 설계:** `allOf`, `anyOf`와 같은 키워드를 활용하여, 데이터의 변화에 유연하게 대응할 수 있는 스키마를 설계합니다. (부모 노드 '필드 기본값과 모범 사례' 참고)
*   **실용적인 예시:** 쇼핑몰 상품 데이터 검증 시, 각 카테고리에 따라 다른 필수 필드를 요구하도록 스키마를 구성하는 예시를 제시합니다. (부모 노드의 '추가적인 세부 사항 및 예시' 참고)

## 주요 화제
- **외부 웹 서비스 데이터 검증 필요성**: 외부 웹 서비스 (예: Open Library Books API)에서 제공하는 JSON 형식의 데이터를 검증해야 하는 이유와 그 중요성에 대한 논의.
- **JSON Schema 기초**: JSON Schema의 기본 개념, 구조, 그리고 외부 API 응답에 대한 스키마 작성 (예: "Watchmen" 책 정보에 대한 스키마)
- **JSON Schema composition (스키마 조합)**:
- JSON Schema를 사용하여 여러 스키마를 조합하는 방법 (allOf, anyOf 활용)
- isbn_10과 isbn_13 필드의 유효성 검증을 위한 스키마 조합 예시 (basicBookInfoSchema, mandatoryIsbn13, mandatoryIsbn10)
- 스키마 조합을 통한 데이터 유효성 검증 표현력 확장
- **JSON Schema의 표현력과 한계**: 클래스를 사용한 데이터 표현 방식과 비교하여 JSON Schema의 표현력의 장점 및 한계 (유효성 검증 실패 시 구체적인 오류 정보 부족).
- **JSON Schema 추가 기능**: oneOf와 같은 JSON Schema의 추가 기능에 대한 언급.

## 부차 화제
- 외부 웹 서비스 데이터 검증 필요성: 외부 API로부터 받은 데이터의 유효성을 검증해야 하는 필요성에 대한 논의
- JSON Schema를 이용한 데이터 검증: JSON Schema를 사용하여 데이터의 구조와 유효성을 정의하고 검증하는 방법 설명
- ISBN 필드의 조건부 필수 속성: isbn_10 및 isbn_13 필드가 모든 책에 항상 존재하는 것이 아니라, 출판 연도에 따라 조건부로 존재하는 경우를 설명하고, 이를 JSON Schema로 표현하는 방법 논의
- JSON Schema composition: 여러 개의 JSON Schema를 결합하여 복잡한 데이터 구조를 표현하는 방법 (allOf, anyOf)
- JSON Schema의 표현력: 클래스를 사용하여 데이터를 표현하는 것보다 JSON Schema가 더 많은 표현력을 가질 수 있다는 점을 언급
- 데이터 검증 실패 시 문제점: 유효성 검증 실패 시, 어떤 부분이 잘못되었는지 명확하게 알 수 없는 점에 대한 문제 제기
- oneOf: JSON Schema에서 데이터가 정확히 하나의 스키마에만 유효해야 하는 경우를 나타내는 `oneOf` 기능 소개
- 고급 데이터 유효성 검증: 챕터 12에서 다룰 고급 데이터 유효성 검증에 대한 언급

# 내용
---
## 7.4 Schema composition

7.4 Schema composition
Theo What about validating data that comes from an external web service?
Joe Can you give me an example?
Theo In the near future, we'll have to integrate with a service called Open Library
Books API that provides detailed information about books.
 NOTE For information on the Open Library Books API, see https://openlibrary
.org/dev/docs/api/books.
Joe Can you show me, for instance, the service response for Watchmen?
Theo Sure. Here you go.
Theo taps a few keys on his keyboard and brings up the response. Joe looks at the JSON for
a long time.
Listing7.16 An Open Library Books API response example
{
"publishers": [
"DC Comics"
],
"number_of_pages": 334,
"weight": "1.4 pounds",
"physical_format": "Paperback",
"subjects": [
"Graphic Novels",
"Comics & Graphic Novels",
"Fiction",
"Fantastic fiction"
],
"isbn_13": [
"9780930289232"
],
"title": "Watchmen",
"isbn_10": [
"0930289234"
],
"publish_date": "April 1, 1995",
"physical_dimensions": "10.1 x 6.6 x 0.8 inches"
}
Theo asks himself, "What could be so special in this JSON?" While Joe is meditating about
this piece of JSON, Theo writes the JSON Schema for the Books API response. It doesn't
seem to be more complicated than any of the previous schemas. When Theo is done, he
asks Joe to take a look at the schema.
Listing7.17 Schema of the Open Library Books API response
{
"type": "object",
"required": ["title"],

=== 페이지 183 ===
7.4 Schema composition 155
"properties": {
"title": {"type": "string"},
"publishers": {
"type": "array",
"items": {"type": "string"}
},
"number_of_pages": {"type": "integer"},
"weight": {"type": "string"},
"physical_format": {"type": "string"},
"subjects": {
"type": "array",
"items": {"type": "string"}
},
"isbn_13": {
"type": "array",
"items": {"type": "string"}
},
"isbn_10": {
"type": "array",
"items": {"type": "string"}
},
"publish_date": {"type": "string"},
"physical_dimensions": {"type": "string"}
}
}
Joe Good job!
Theo That wasn't so hard. I really don't see why you looked at this JSON response for
such a long time.
Joe Well, it has to do with the isbn_10 and isbn_13 fields. I assume that they're
not both mandatory.
Theo Right! That's why I didn't include them in the required field of my schema.
Joe But one of them should always be there. Right?
Theo Sometimes one of them and sometimes both of them, like for Watchmen. It
depends on the publication year of the book. Books published before 2007
have isbn_10, and books published after 2007 have isbn_13.
Joe Oh, I see. And Watchmen has both because it was originally published in 1986
but published again after 2007.
Theo Correct.
Joe Then, you need your schema to indicate that one of the isbn fields is man-
datory. That's a good opportunity for me to tell you about JSON Schema
composition.
Theo What's that?
Joe It's a way to combine schemas, similarly to how we combine logical conditions
with AND, OR, and NOT.
Theo I'd like to see that.
Joe Sure. How would you express the schema for the Books API response as a
composition of three schemas: basicBookInfoSchema, the schema that you
wrote where only title is required; mandatoryIsbn13, a schema where only

=== 페이지 184 ===
156 CHAPTER 7 Basic data validation
isbn_13 is required; and mandatoryIsb10, a schema where only isbn_10 is
required?
Theo I think it should be basicBookInfoSchema AND (mandatoryIsbn13 OR
mandatoryIsbn10).
Joe Exactly! The only thing is that in JSON Schema, we use allOf instead of AND,
and anyOf instead of OR.
Joe shows Theo the result in listing 7.18 and an example of its usage in listing 7.19.
Listing7.18 Schema of an external API response
var basicBookInfoSchema = {
"type": "object",
"required": ["title"],
"properties": {
"title": {"type": "string"},
"publishers": {
"type": "array",
"items": {"type": "string"}
},
"number_of_pages": {"type": "integer"},
"weight": {"type": "string"},
"physical_format": {"type": "string"},
"subjects": {
"type": "array",
"items": {"type": "string"}
},
"isbn_13": {
"type": "array",
"items": {"type": "string"}
},
"isbn_10": {
"type": "array",
"items": {"type": "string"}
},
"publish_date": {"type": "string"},
"physical_dimensions": {"type": "string"}
}
};
var mandatoryIsbn13 = {
"type": "object",
"required": ["isbn_13"]
};
var mandatoryIsbn10 = {
"type": "object",
"required": ["isbn_10"]
};
var bookInfoSchema = {
"allOf": [
basicBookInfoSchema,
{

=== 페이지 185 ===
7.4 Schema composition 157
"anyOf": [mandatoryIsbn13, mandatoryIsbn10]
}
]
};
Listing7.19 Validating an external API response
var bookInfo = {
"publishers": [
"DC Comics"
],
"number_of_pages": 334,
"weight": "1.4 pounds",
"physical_format": "Paperback",
"subjects": [
"Graphic Novels",
"Comics & Graphic Novels",
"Fiction",
"Fantastic fiction"
],
"isbn_13": [
"9780930289232"
],
"title": "Watchmen",
"isbn_10": [
"0930289234"
],
"publish_date": "April 1, 1995",
"physical_dimensions": "10.1 x 6.6 x 0.8 inches"
};
validate(bookInfoSchema, bookInfo);
// → true
Theo I see why they call it allOf and anyOf. The first one means that data must con-
form to all the schemas, and the second one means that data must conform to
any of the schemas.
Joe Yup.
 NOTE JSON Schema also supports oneOf for cases where data must be valid against
exactly one schema.
Theo Nice. With schema composition, JSON Schema seems to have more expressive
power than what I was used to when representing data with classes.
Joe That's only the beginning. I'll show you more data validation conditions that
can't be expressed when data is represented with classes some other time.
 NOTE Advanced data validation is covered in chapter 12.
Theo Something still bothers me, though. When data isn't valid, you don't know what
went wrong.

=== 페이지 186 ===
158 CHAPTER 7 Basic data validation

# 구성
---
없음 (리프 노드)
