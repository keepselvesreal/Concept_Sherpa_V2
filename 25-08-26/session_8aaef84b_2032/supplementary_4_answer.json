{
  "query_number": 4,
  "outputs": {
    "relevance_analysis": {
      "is_relevant": true,
      "description": "현재 질의는 직전 순수 함수 설명과 직접적으로 연관된 심화 질문입니다. 사용자는 순수 함수의 '부작용 없음' 조건과 '상태(state)' 개념 사이의 관계를 명확히 하고자 합니다. 직전 답변에서 '외부 상태 변경 금지'를 언급했는데, 사용자는 함수 내부의 지역 상태나 임시 변수 사용도 순수성을 해치는지에 대해 궁금해하는 것으로 보입니다.",
      "generated_query": "순수 함수에서 함수 내부의 지역 변수나 임시 상태는 허용되는가? 외부 상태와 내부 상태의 차이점, 그리고 immutable vs mutable 데이터 사용이 순수성에 미치는 영향은?"
    },
    "supplementary_response": {
      "query": "순수 함수에서 함수 내부의 지역 변수나 임시 상태는 허용되는가? 외부 상태와 내부 상태의 차이점, 그리고 immutable vs mutable 데이터 사용이 순수성에 미치는 영향은?",
      "response": "태수야, 순수 함수에 대한 좋은 질문이네! 참조 문서에서 함수형 프로그래밍의 순수 함수 개념이 나와 있으니, 이를 바탕으로 자세히 설명해줄게.\n\n## 순수 함수에서 내부 지역 변수는 완전히 허용돼\n\n```python\ndef calculate_average(numbers):\n    # 이런 지역 변수들은 전혀 문제없어\n    total = 0  # 지역 변수\n    count = len(numbers)  # 지역 변수\n    temp_list = []  # 임시 상태\n    \n    for num in numbers:\n        total += num\n        temp_list.append(num * 2)  # 내부에서만 사용\n    \n    return total / count if count > 0 else 0\n```\n\n**핵심은**: 함수 외부에서 이 지역 변수들에 접근할 수 없고, 함수 실행이 끝나면 사라진다는 점이야.\n\n## 외부 상태 vs 내부 상태의 차이\n\n### ❌ 외부 상태 (순수성 위반)\n```python\nglobal_counter = 0  # 전역 변수\n\ndef impure_function(x):\n    global global_counter\n    global_counter += 1  # 외부 상태 변경!\n    return x + global_counter  # 결과가 매번 달라짐\n```\n\n### ✅ 내부 상태 (순수함수 OK)\n```python\ndef pure_function(x, y):\n    # 내부에서만 사용되는 변수들\n    intermediate_result = x * 2\n    temp_calculation = y + 10\n    local_sum = intermediate_result + temp_calculation\n    \n    return local_sum  # 같은 입력이면 항상 같은 출력\n```\n\n## Immutable vs Mutable 데이터의 영향\n\n참조 문서에서 언급한 대로, 함수형 프로그래밍에서는 **불변성**이 중요해.\n\n### 내부에서 Mutable 데이터를 사용해도 순수함수가 될 수 있어\n```python\ndef process_data(input_list):\n    # mutable 데이터를 내부에서 사용\n    result = []  # mutable list\n    temp_dict = {}  # mutable dict\n    \n    for item in input_list:\n        temp_dict[item] = item * 2\n        result.append(temp_dict[item])\n    \n    return result  # 새로운 데이터 반환\n```\n\n**중요한 건**: \n1. 입력 데이터를 변경하지 않음\n2. 외부 상태를 변경하지 않음  \n3. 같은 입력에 대해 항상 같은 출력\n\n### 진짜 문제가 되는 경우\n```python\ndef bad_function(my_list):\n    my_list.append(999)  # 입력 데이터 변경! (부작용)\n    return sum(my_list)\n```\n\n## 실용적인 조언\n\n참조 문서의 DOP 내용처럼, **데이터 변환**에 집중해봐:\n\n```python\ndef good_data_transformation(user_data):\n    # 내부에서 임시 변수 사용 OK\n    processed_users = []\n    \n    for user in user_data:\n        # 원본 데이터는 건드리지 않고 새로운 데이터 생성\n        enhanced_user = {\n            **user,  # 기존 데이터 복사\n            \"is_adult\": user[\"age\"] >= 18,\n            \"display_name\": user[\"name\"].upper()\n        }\n        processed_users.append(enhanced_user)\n    \n    return processed_users\n```\n\n**결론적으로**: 순수 함수는 \"외부와의 관계\"가 순수하면 되는 거야. 내부에서 뭘 하든 상관없어 - 지역 변수든, mutable 데이터든. 다만 입력을 변경하거나 외부 상태에 영향을 주면 안 돼.\n\n이해됐어? 더 궁금한 부분 있으면 언제든 물어봐!",
      "has_relevant_content": true,
      "elapsed_time": 29.15,
      "timestamp": "2025-08-26T12:14:16.595862+00:00",
      "document_paths": [
        "references/OOP.md",
        "references/FP.md",
        "references/DOP.md"
      ],
      "success": true
    }
  }
}