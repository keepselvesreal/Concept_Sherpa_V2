# 2 Separation between code and data

<구성 노드 반영 완료>

## Core 내용
코드와 데이터 분리를 핵심으로 하는 DOP(Data-Oriented Programming) 시스템은 시스템 복잡성을 줄이고 유연성을 높이는 프로그래밍 패러다임입니다. 데이터는 데이터 엔티티로 표현되고, 코드는 상태를 갖지 않는(stateless) 함수들의 집합인 코드 모듈로 구성됩니다.

## 상세 핵심 내용
본 문서는 "27 lev1 2 Separation between code and data"라는 제목으로, DOP 시스템의 핵심 원리를 설명합니다. DOP는 OOP의 데이터 캡슐화를 지양하고, 코드와 데이터의 분리를 통해 시스템의 이해도와 유연성을 향상시킵니다. 데이터 엔티티는 시스템의 정보를 담고, 코드 모듈은 데이터 엔티티를 조작하는 역할을 수행합니다. DOP는 특정 언어나 패러다임에 종속되지 않으며, OOP와 FP 모두에서 적용 가능합니다. 본 문서에서는 DOP의 개념, 장점, 데이터 엔티티 및 코드 모듈의 설계, 시스템의 이해 용이성 및 유연성에 대해 다룹니다. 특히 도서관 관리 시스템을 예시로, 데이터 엔티티 식별, 그룹화, 시각화 방법과 코드 모듈의 설계 원리를 구체적으로 제시합니다.

## 상세 정보 내용
본 문서는 DOP 시스템의 설계와 구현에 대한 내용을 담고 있으며, 각 구성 요소의 특징과 장점을 상세하게 설명합니다. 
-   **28 lev2 2.1 The two parts of a DOP system**: DOP의 핵심 원리, DOP와 OOP의 차이점, DOP의 장점 (복잡성 감소, 유연성 증가), DOP의 언어 독립성.
-   **29 lev2 2.2 Data entities**: DOP에서의 데이터 엔티티 개념, 데이터 엔티티 식별 및 그룹화 방법, 도서관 관리 시스템 예시, 데이터 엔티티의 시각화 방법.
-   **30 lev2 2.3 Code modules**: DOP에서의 코드 모듈 정의, 코드 모듈 설계 원칙 (stateless, explicit data argument), OOP와의 비교, 고수준 모듈 구성.
-   **31 lev2 2.4 DOP systems are easy to understand**: DOP 시스템의 이해 용이성, DOP 시스템의 구성 요소 (코드 모듈, 데이터 엔티티), DOP 모듈 다이어그램의 특징, DOP에서의 다형성 구현 방식.
-   **32 lev2 2.5 DOP systems are flexible**: DOP 시스템의 유연성, 요구 사항 변경에 대한 적응성, Super 멤버 및 VIP 멤버 기능 구현 예시.
-   **33 lev2 Summary**: DOP의 원리, 코드와 데이터 분리, DOP 시스템의 장점, 데이터 엔티티와 코드 모듈의 역할 및 관계.

## 주요 화제
*   코드와 데이터의 분리 (DOP의 핵심 원리)
*   DOP (Data-Oriented Programming) 시스템의 개념, 장점, 설계 원리
*   데이터 엔티티 (식별, 그룹화, 시각화)
*   코드 모듈 (stateless, 데이터 엔티티 기반)
*   DOP 시스템의 이해 용이성 및 유연성
*   도서관 관리 시스템을 예시로 한 DOP 적용

## 부차 화제
*   OOP(객체 지향 프로그래밍)과의 비교
*   FP(함수형 프로그래밍)과의 관계
*   데이터 엔티티 관계 (연관 또는 구성)
*   데이터 엔티티 디자인의 반복적 성격
*   DOP 시스템의 API 설계
*   모듈과 클래스의 관계
*   Python에서의 self와 DOP의 데이터 전달 방식 비교
*   DOP 시스템의 Polymorphism 구현 방식
*   에러 관리 (챕터 3)
*   데이터 쿼리 (챕터 3)
*   시스템 상태 관리 (챕터 4)

<부모 노드 반영 완료>

## Core 내용
DOP 시스템은 코드와 데이터 분리를 핵심으로 하여 시스템 복잡성을 줄이고 유연성을 높이는 프로그래밍 패러다임입니다. OOP의 데이터 캡슐화를 지양하고, 데이터 엔티티와 코드 모듈로 시스템을 구성합니다.

## 상세 핵심 내용
DOP는 데이터 엔티티와 코드 모듈의 분리를 통해 시스템의 이해도와 유연성을 향상시킵니다. 데이터 엔티티는 시스템의 정보를 담고, 코드 모듈은 상태를 갖지 않는 함수들의 집합으로 데이터 엔티티를 조작합니다. 본 챕터에서는 DOP의 핵심 원리, DOP와 OOP의 차이점, DOP의 장점 (복잡성 감소, 유연성 증가), DOP의 언어 독립성을 설명합니다.

## 상세 정보 내용
본 챕터는 DOP 시스템의 두 가지 주요 부분, 즉 데이터 엔티티와 코드 모듈의 개념과 특징을 설명합니다. DOP의 핵심 원리와 OOP와의 차이점을 비교하고, DOP의 장점과 언어 독립성을 상세히 다룹니다. 또한, 실제 라이브러리 관리 시스템 개발 경험과 객체 지향 프로그래밍(OOP)의 문제점을 간략하게 언급합니다.

## 주요 화제
*   데이터 지향 프로그래밍(DOP)의 개념
*   DOP의 핵심 원리 (코드와 데이터 분리, 데이터 캡슐화 지양)
*   DOP의 장점 (시스템 복잡성 감소, 유연성 증가)
*   DOP의 언어 독립성

## 부차 화제
*   Theo의 라이브러리 관리 시스템 개발 경험
*   객체 지향 프로그래밍(OOP)과 데이터 캡슐화의 문제점
*   함수형 프로그래밍(FP)과의 관계
*   Theo와 Joe의 대화 형식

<부모 노드 반영 완료>

## Core 내용
DOP에서 데이터 엔티티는 시스템의 정보를 담는 역할을 하며, 시스템 요구 사항에서 명사 및 명사구를 식별하여 정의합니다. 데이터 엔티티는 시스템을 이해하고 시각화하는 데 중요한 역할을 합니다.

## 상세 핵심 내용
DOP에서 데이터 엔티티는 시스템의 정보를 표현하며, 시스템의 구조를 파악하는 데 핵심적인 역할을 합니다. 데이터 엔티티 식별 및 그룹화 방법과 도서관 관리 시스템을 예시로 한 데이터 엔티티 분석을 설명합니다. 데이터 엔티티는 마인드 맵, 중첩 목록, 데이터 엔티티 다이어그램과 같은 다양한 방법으로 시각화할 수 있습니다.

## 상세 정보 내용
본 챕터는 DOP에서의 데이터 엔티티 개념, 식별 및 그룹화 방법, 시각화 방법을 상세히 설명합니다. 도서관 관리 시스템을 예시로, 실제 적용 사례를 제시합니다. 데이터 엔티티의 관계, 디자인의 반복적 성격에 대한 간략한 언급도 포함합니다.

## 주요 화제
*   DOP(Design-Oriented Programming)에서의 데이터 엔티티 개념
*   데이터 엔티티 식별 및 그룹화 방법
*   도서관 관리 시스템을 예시로 한 데이터 엔티티 분석
*   데이터 엔티티의 시각화 방법 (마인드 맵, 중첩 목록, 데이터 엔티티 다이어그램)

## 부차 화제
*   데이터 엔티티 관계 (연관 또는 구성)에 대한 간략한 언급
*   데이터 엔티티 디자인의 반복적 성격 (초기 단계에서는 세부 사항에 너무 신경 쓰지 않음)

<부모 노드 반영 완료>

## Core 내용
DOP에서 코드 모듈은 상태를 갖지 않는 함수들의 집합으로, 데이터 엔티티를 조작하는 역할을 합니다. 각 함수는 데이터 엔티티를 인자로 받으며, 데이터 중심 설계가 핵심입니다.

## 상세 핵심 내용
DOP에서 코드 모듈은 시스템의 기능을 나타내는 함수들을 포함하며, 각 함수는 데이터 엔티티를 첫 번째 인자로 받습니다. 코드 모듈은 상태를 갖지 않으며, 데이터 중심 설계를 통해 모듈 간의 상호 작용을 명확하게 정의합니다. 본 챕터에서는 코드 모듈 설계 원칙, OOP와의 비교, 고수준 모듈 구성에 대해 다룹니다.

## 상세 정보 내용
본 챕터는 DOP에서의 코드 모듈 정의, 설계 원칙, OOP와의 비교, 고수준 모듈 구성에 대한 내용을 담고 있습니다. Python에서의 데이터 전달 방식과 라이브러리 관리 시스템 예시를 통해 구체적인 이해를 돕습니다.

## 주요 화제
*   DOP에서의 코드 모듈 정의
*   DOP 코드 모듈 설계 원칙 (stateless, explicit data argument)
*   전통적인 OOP와 DOP에서의 함수 인자 전달 방식 비교
*   고수준 모듈 구성 (데이터 엔티티 기반)

## 부차 화제
*   DOP 시스템의 API 설계
*   모듈과 클래스의 관계
*   Python에서의 self와 DOP의 데이터 전달 방식 비교
*   Library Management System의 예시

<부모 노드 반영 완료>

## Core 내용
DOP 시스템은 코드 모듈과 데이터 엔티티의 명확한 분리를 통해 시스템의 이해도를 높입니다. DOP 모듈 다이어그램은 단순하며, 다형성은 상속이 아닌 다른 메커니즘을 통해 구현됩니다.

## 상세 핵심 내용
DOP 시스템은 코드와 데이터의 분리를 통해 이해 용이성을 확보합니다. 데이터 엔티티와 코드 모듈의 역할을 명확히 구분하여 시스템의 복잡성을 줄입니다. DOP 모듈 다이어그램의 특징과 단순성을 설명하고, DOP에서의 다형성 구현 방식을 제시합니다.

## 상세 정보 내용
본 챕터는 DOP 시스템의 이해 용이성을 설명하고, DOP 시스템과 전통적인 OOP 시스템을 비교합니다. DOP 시스템의 구성 요소, DOP 모듈 다이어그램의 특징, DOP에서의 다형성 구현 방식 등을 상세히 다룹니다.

## 주요 화제
*   DOP(Data-Oriented Programming) 시스템의 이해 용이성
*   DOP 시스템과 전통적인 OOP 시스템의 비교
*   DOP 시스템의 구성 요소 (코드 모듈, 데이터 엔티티)
*   DOP 모듈 다이어그램의 특징과 단순성
*   DOP에서의 다형성 구현 방식

## 부차 화제
*   데이터 마인드 맵과 모듈 다이어그램
*   라이브러리 관리 시스템 예시
*   함수의 정적(stateless) 특성
*   모듈 간의 관계 제약 조건
*   DOP에서의 polymorphism 구현 방식

<부모 노드 반영 완료>

## Core 내용
DOP 시스템은 코드와 데이터의 분리를 통해 유연성을 확보하여, 요구 사항 변경에 쉽게 대응할 수 있습니다. Super 멤버 및 VIP 멤버 기능 구현 예시를 통해 유연성을 보여줍니다.

## 상세 핵심 내용
DOP 시스템은 코드와 데이터의 분리를 통해 요구 사항 변경에 대한 높은 적응성을 제공합니다. 새로운 기능을 추가할 때 기존 코드를 수정하거나 함수를 추가하는 방식으로 쉽게 확장할 수 있습니다. 본 챕터에서는 DOP 시스템의 유연성과 Super 멤버 및 VIP 멤버 기능 구현 예시를 제시합니다.

## 상세 정보 내용
본 챕터는 DOP 시스템의 유연성과 요구 사항 변경에 대한 적응성을 설명합니다. OOP 시스템과의 비교를 통해 DOP의 장점을 강조하고, Super 멤버 및 VIP 멤버 기능 구현 예시를 통해 실제 적용 사례를 보여줍니다.

## 주요 화제
*   DOP 시스템의 유연성
*   요구 사항 변경에 대한 DOP 시스템의 적응성
*   OOP 시스템과의 비교
*   Super 멤버 및 VIP 멤버 기능 구현 예시

## 부차 화제
*   DOP에서의 함수 및 데이터 객체 사용
*   에러 관리 (챕터 3에서 다룸)
*   데이터 쿼리 (챕터 3에서 다룸)
*   시스템 상태 관리 (챕터 4에서 다룸)

<부모 노드 반영 완료>

## Core 내용
DOP는 코드와 데이터 분리를 핵심으로 하는 프로그래밍 패러다임으로, 시스템의 유연성과 단순성을 향상시킵니다. 데이터는 데이터 엔티티로, 코드는 stateless functions로 구성됩니다.

## 상세 핵심 내용
본 챕터는 DOP의 핵심 원리를 요약하고, 코드와 데이터 분리의 중요성을 강조합니다. DOP 시스템의 장점, 데이터 엔티티와 코드 모듈의 역할 및 관계를 명확히 설명합니다. 또한, OOP와의 비교 및 다형성 구현 방식을 간략하게 언급합니다.

## 상세 정보 내용
본 챕터는 DOP의 원리, 코드와 데이터 분리, DOP 시스템의 장점, 데이터 엔티티와 코드 모듈의 역할 및 관계를 요약합니다. DOP와 OOP 비교, stateless functions, 데이터 캡슐화에 대한 간략한 설명을 제공합니다.

## 주요 화제
*   DOP (Data-Oriented Programming)의 원리
*   코드와 데이터의 분리
*   DOP 시스템의 장점 (유연성, 단순성, 변경 용이성)
*   데이터 엔티티와 코드 모듈의 역할 및 관계

## 부차 화제
*   DOP와 OOP 비교
*   Stateless functions
*   데이터 캡슐화
*   Polymorphism (챕터 13 참조)