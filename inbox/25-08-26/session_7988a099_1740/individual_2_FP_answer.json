{
  "query_number": 2,
  "query": "좀 더 자세한 구체적인 예시를 보여줘",
  "model_response": "태수야, FP.md 문서의 내용을 기반으로 좀 더 구체적이고 실용적인 예시들을 보여줄게:\n\n## 🔧 실제 개발 시나리오별 예시\n\n### 1. 데이터 처리 파이프라인\n```python\n# 학생 성적 데이터 처리 예시\nstudents = [\n    {'name': '김철수', 'scores': [80, 90, 75]},\n    {'name': '이영희', 'scores': [95, 88, 92]},\n    {'name': '박민수', 'scores': [70, 65, 80]}\n]\n\n# 순수 함수들\ndef calculate_average(scores):\n    return sum(scores) / len(scores)\n\ndef add_grade(student):\n    avg = calculate_average(student['scores'])\n    return {**student, 'average': avg, 'grade': 'A' if avg >= 90 else 'B' if avg >= 80 else 'C'}\n\ndef is_honor_student(student):\n    return student['average'] >= 90\n\n# 함수 조합으로 처리 파이프라인 구성\nresult = list(\n    filter(is_honor_student,\n        map(add_grade, students))\n)\n# 결과: 평균 90점 이상인 우등생만 필터링\n```\n\n### 2. 웹 API 응답 처리\n```python\n# API 응답 데이터 변환 예시\napi_response = {\n    'users': [\n        {'id': 1, 'first_name': '태수', 'last_name': '김', 'active': True},\n        {'id': 2, 'first_name': '영희', 'last_name': '이', 'active': False}\n    ]\n}\n\n# 순수 함수들\ndef format_full_name(user):\n    return {**user, 'full_name': f\"{user['last_name']}{user['first_name']}\"}\n\ndef is_active_user(user):\n    return user['active']\n\ndef extract_display_info(user):\n    return {'id': user['id'], 'name': user['full_name'], 'status': 'active'}\n\n# 함수 조합\nactive_users = list(\n    map(extract_display_info,\n        filter(is_active_user,\n            map(format_full_name, api_response['users'])))\n)\n```\n\n### 3. 복잡한 비즈니스 로직\n```python\n# 주문 처리 시스템 예시\nfrom functools import reduce\nfrom typing import List, Dict\n\ndef apply_discount(item, discount_rate=0.1):\n    return {**item, 'discounted_price': item['price'] * (1 - discount_rate)}\n\ndef calculate_tax(item, tax_rate=0.1):\n    price = item.get('discounted_price', item['price'])\n    return {**item, 'tax': price * tax_rate}\n\ndef add_shipping_cost(total, shipping=5000):\n    return total + shipping\n\n# 주문 처리 함수 조합\ndef process_order(items: List[Dict]) -> Dict:\n    # 각 아이템에 할인과 세금 적용\n    processed_items = list(map(\n        lambda item: calculate_tax(apply_discount(item)),\n        items\n    ))\n    \n    # 총 가격 계산\n    subtotal = reduce(\n        lambda total, item: total + item.get('discounted_price', item['price']) + item['tax'],\n        processed_items,\n        0\n    )\n    \n    # 배송비 추가\n    final_total = add_shipping_cost(subtotal)\n    \n    return {\n        'items': processed_items,\n        'subtotal': subtotal,\n        'total': final_total\n    }\n```\n\n### 4. 함수 합성을 활용한 유틸리티\n```python\n# 함수 합성 헬퍼\ndef compose(*functions):\n    return lambda x: reduce(lambda acc, f: f(acc), functions, x)\n\n# 문자열 처리 파이프라인\ndef trim_whitespace(text):\n    return text.strip()\n\ndef to_lowercase(text):\n    return text.lower()\n\ndef replace_spaces(text):\n    return text.replace(' ', '_')\n\n# 함수들을 합성한 새로운 함수\nprocess_slug = compose(trim_whitespace, to_lowercase, replace_spaces)\n\n# 사용\nresult = process_slug(\"  Hello World  \")  # \"hello_world\"\n```\n\n## 🎯 OOP와 비교한 실제 차이점\n\n### OOP 방식:\n```python\nclass ShoppingCart:\n    def __init__(self):\n        self.items = []\n        self.total = 0\n    \n    def add_item(self, item):  # 상태 변경\n        self.items.append(item)\n        self.total += item['price']\n    \n    def apply_discount(self, rate):  # 기존 데이터 변경\n        self.total *= (1 - rate)\n```\n\n### FP 방식:\n```python\ndef add_item(cart, item):  # 새로운 cart 반환\n    return {\n        'items': cart['items'] + [item],\n        'total': cart['total'] + item['price']\n    }\n\ndef apply_discount(cart, rate):  # 새로운 cart 반환\n    return {**cart, 'total': cart['total'] * (1 - rate)}\n```\n\n이렇게 보면 FP가 더 예측 가능하고 테스트하기 쉬운 걸 알 수 있어. 특히 복잡한 데이터 변환이나 비동기 처리에서는 FP 방식이 훨씬 안전하고 관리하기 좋아.",
  "has_relevant_content": true,
  "elapsed_time": 49.58439779281616,
  "timestamp": "2025-08-26T17:41:50.348460",
  "document_path": "/home/nadle/projects/Knowledge_Sherpa/v2/25-08-26/references/FP.md",
  "success": true
}