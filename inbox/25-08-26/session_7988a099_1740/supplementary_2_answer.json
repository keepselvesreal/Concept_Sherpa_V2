{
  "query_number": 2,
  "outputs": {
    "relevance_analysis": {
      "is_relevant": true,
      "description": "현재 질의는 직전 답변과 직접적으로 연관되어 있음. 사용자가 객체지향 프로그래밍의 4가지 특징에 대한 설명을 들었지만, 이론적인 설명만으로는 이해가 부족하여 구체적인 예시와 실제 코드를 통한 시연을 요청하고 있음. 이는 학습 심화 단계로 자연스러운 후속 질문임.",
      "generated_query": "객체지향 프로그래밍의 캡슐화, 상속, 다형성, 추상화를 각각 실제 Python 코드 예시로 보여주고, 각 특징이 어떻게 코드에서 구현되는지와 실무에서 어떤 상황에 활용되는지 설명해주세요"
    },
    "supplementary_response": {
      "query": "객체지향 프로그래밍의 캡슐화, 상속, 다형성, 추상화를 각각 실제 Python 코드 예시로 보여주고, 각 특징이 어떻게 코드에서 구현되는지와 실무에서 어떤 상황에 활용되는지 설명해주세요",
      "response": "태수야, 객체지향 프로그래밍의 4가지 핵심 원리를 Python 코드로 설명해줄게. 실무에서 어떻게 쓰이는지도 함께 보자.\n\n## 1. 캡슐화 (Encapsulation)\n\n```python\nclass BankAccount:\n    def __init__(self, initial_balance):\n        self.__balance = initial_balance  # private 속성\n    \n    def deposit(self, amount):\n        if amount > 0:\n            self.__balance += amount\n            return True\n        return False\n    \n    def withdraw(self, amount):\n        if 0 < amount <= self.__balance:\n            self.__balance -= amount\n            return True\n        return False\n    \n    def get_balance(self):\n        return self.__balance\n\n# 사용\naccount = BankAccount(1000)\naccount.deposit(500)\nprint(account.get_balance())  # 1500\n# account.__balance  # 오류! 직접 접근 불가\n```\n\n**실무 활용**: API 클라이언트 라이브러리에서 인증 토큰을 숨기거나, 데이터베이스 연결 정보를 캡슐화할 때 많이 써. 외부에서 중요한 데이터를 함부로 건드리지 못하게 하는 거지.\n\n## 2. 상속 (Inheritance)\n\n```python\nclass Vehicle:\n    def __init__(self, brand, model):\n        self.brand = brand\n        self.model = model\n    \n    def start(self):\n        print(f\"{self.brand} {self.model} 시동을 걸었습니다.\")\n    \n    def stop(self):\n        print(f\"{self.brand} {self.model} 정지했습니다.\")\n\nclass Car(Vehicle):\n    def __init__(self, brand, model, doors):\n        super().__init__(brand, model)\n        self.doors = doors\n    \n    def honk(self):\n        print(\"빵빵!\")\n\nclass Motorcycle(Vehicle):\n    def __init__(self, brand, model, cc):\n        super().__init__(brand, model)\n        self.cc = cc\n    \n    def wheelie(self):\n        print(\"앞바퀴 들기!\")\n\n# 사용\ncar = Car(\"현대\", \"소나타\", 4)\ncar.start()  # 부모 메서드 사용\ncar.honk()   # 고유 메서드\n\nbike = Motorcycle(\"혼다\", \"CBR\", 600)\nbike.start()  # 같은 부모 메서드지만 다른 객체\nbike.wheelie()\n```\n\n**실무 활용**: Django의 모델 클래스나 웹 프레임워크의 기본 클래스를 상속받아서 커스텀 기능을 만들 때 핵심이야. 공통 기능은 부모에서, 특화 기능은 자식에서 구현하는 식으로.\n\n## 3. 다형성 (Polymorphism)\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass Animal(ABC):\n    @abstractmethod\n    def make_sound(self):\n        pass\n    \n    @abstractmethod\n    def move(self):\n        pass\n\nclass Dog(Animal):\n    def make_sound(self):\n        return \"멍멍\"\n    \n    def move(self):\n        return \"네 발로 뛰어다닙니다\"\n\nclass Bird(Animal):\n    def make_sound(self):\n        return \"짹짹\"\n    \n    def move(self):\n        return \"날아다닙니다\"\n\nclass Fish(Animal):\n    def make_sound(self):\n        return \"...\"  # 물고기는 소리를 내지 않음\n    \n    def move(self):\n        return \"헤엄칩니다\"\n\n# 다형성 활용\ndef animal_actions(animals):\n    for animal in animals:\n        print(f\"소리: {animal.make_sound()}\")\n        print(f\"움직임: {animal.move()}\")\n        print(\"-\" * 20)\n\n# 사용\nanimals = [Dog(), Bird(), Fish()]\nanimal_actions(animals)  # 같은 인터페이스로 다른 동작\n```\n\n**실무 활용**: 결제 시스템에서 신용카드, 페이팔, 카카오페이 등 다양한 결제 방식을 하나의 인터페이스로 처리할 때 써. 각각 다른 구현이지만 같은 방식으로 호출할 수 있어서 코드가 깔끔해져.\n\n## 4. 추상화 (Abstraction)\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass DatabaseConnection(ABC):\n    @abstractmethod\n    def connect(self):\n        pass\n    \n    @abstractmethod\n    def execute_query(self, query):\n        pass\n    \n    @abstractmethod\n    def close(self):\n        pass\n\nclass MySQLConnection(DatabaseConnection):\n    def __init__(self, host, username, password):\n        self.host = host\n        self.username = username\n        self.password = password\n        self.connection = None\n    \n    def connect(self):\n        # 복잡한 MySQL 연결 로직 숨김\n        print(f\"MySQL {self.host}에 연결 중...\")\n        self.connection = \"mysql_connection_object\"\n        return True\n    \n    def execute_query(self, query):\n        if not self.connection:\n            raise Exception(\"연결이 필요합니다\")\n        print(f\"MySQL 쿼리 실행: {query}\")\n        return \"mysql_result\"\n    \n    def close(self):\n        print(\"MySQL 연결 종료\")\n        self.connection = None\n\nclass PostgreSQLConnection(DatabaseConnection):\n    def __init__(self, host, username, password):\n        self.host = host\n        self.username = username\n        self.password = password\n        self.connection = None\n    \n    def connect(self):\n        # 복잡한 PostgreSQL 연결 로직 숨김\n        print(f\"PostgreSQL {self.host}에 연결 중...\")\n        self.connection = \"postgresql_connection_object\"\n        return True\n    \n    def execute_query(self, query):\n        if not self.connection:\n            raise Exception(\"연결이 필요합니다\")\n        print(f\"PostgreSQL 쿼리 실행: {query}\")\n        return \"postgresql_result\"\n    \n    def close(self):\n        print(\"PostgreSQL 연결 종료\")\n        self.connection = None\n\n# 추상화 활용 - 복잡한 구현은 숨기고 간단한 인터페이스만 제공\ndef database_operations(db: DatabaseConnection):\n    db.connect()\n    result = db.execute_query(\"SELECT * FROM users\")\n    db.close()\n    return result\n\n# 사용 - 어떤 DB든 같은 방식으로 사용\nmysql_db = MySQLConnection(\"localhost\", \"user\", \"pass\")\npostgresql_db = PostgreSQLConnection(\"localhost\", \"user\", \"pass\")\n\ndatabase_operations(mysql_db)\ndatabase_operations(postgresql_db)\n```\n\n**실무 활용**: 복잡한 외부 API나 라이브러리를 감싸서 팀원들이 쉽게 사용할 수 있게 만들 때 핵심이야. 예를 들어 AWS SDK의 복잡한 설정을 숨기고 간단한 메서드 몇 개로만 사용하게 만드는 식으로.\n\n## 💡 실무에서의 진짜 조언\n\n이 네 가지 원리가 모두 좋다고 해서 무조건 써야 한다는 건 아니야. **과도한 추상화는 오히려 코드를 복잡하게 만들어**. \n\n- 간단한 기능이면 함수 하나로도 충분해\n- 상속보다는 **컴포지션(composition)**이 더 유연할 때가 많아\n- 추상화는 **정말 공통 패턴이 명확할 때**만 써\n\n객체지향은 도구일 뿐이니까, 상황에 맞게 적절히 사용하는 게 중요해. 무작정 클래스부터 만들지 말고, 정말 필요할 때 리팩토링하면서 도입하는 것도 좋은 방법이야.",
      "has_relevant_content": true,
      "elapsed_time": 41.27,
      "timestamp": "2025-08-26T08:42:05.635512+00:00",
      "document_paths": [
        "references/OOP.md",
        "references/FP.md",
        "references/DOP.md"
      ],
      "success": true
    }
  }
}