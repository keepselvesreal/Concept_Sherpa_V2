{
  "query_number": 2,
  "outputs": {
    "relevance_analysis": {
      "is_relevant": true,
      "description": "직전 질의에서 데이터 지향 프로그래밍의 개념과 특징을 설명했고, 현재 질의는 그 내용에 대한 구체적인 예시를 요청하는 명확한 후속 질문입니다. 사용자가 이론적 설명을 듣고 실제 적용 사례나 코드 예시를 통해 더 깊이 이해하고자 하는 자연스러운 학습 진행 과정입니다.",
      "generated_query": "데이터 지향 프로그래밍을 OOP나 함수형 프로그래밍과 비교한 실제 코드 예시를 통해 각 패러다임의 차이점과 DOP의 데이터 우선 설계, 불변성, 데이터-로직 분리가 실제로 어떻게 구현되는지 보여주세요"
    },
    "supplementary_response": {
      "query": "데이터 지향 프로그래밍을 OOP나 함수형 프로그래밍과 비교한 실제 코드 예시를 통해 각 패러다임의 차이점과 DOP의 데이터 우선 설계, 불변성, 데이터-로직 분리가 실제로 어떻게 구현되는지 보여주세요",
      "response": "태수야, 세 가지 패러다임을 실제 코드로 비교해보자. 같은 문제를 다르게 접근하는 방식을 보면 각 패러다임의 철학이 확실히 드러날 거야.\n\n## 실제 예시: 전자상거래 주문 처리 시스템\n\n### 🏢 객체지향 프로그래밍 (OOP)\n\n```python\nclass Product:\n    def __init__(self, id, name, price, stock):\n        self._id = id\n        self._name = name  \n        self._price = price\n        self._stock = stock\n    \n    def reduce_stock(self, quantity):\n        if self._stock >= quantity:\n            self._stock -= quantity  # 상태 변경\n            return True\n        return False\n    \n    def get_price(self):\n        return self._price\n\nclass Order:\n    def __init__(self, customer_id):\n        self.customer_id = customer_id\n        self.items = []\n        self.status = \"pending\"\n    \n    def add_item(self, product, quantity):\n        if product.reduce_stock(quantity):  # 객체 상태 변경\n            self.items.append({\n                'product': product,\n                'quantity': quantity\n            })\n            return True\n        return False\n    \n    def calculate_total(self):\n        total = 0\n        for item in self.items:\n            total += item['product'].get_price() * item['quantity']\n        return total\n    \n    def process(self):\n        if self.items:\n            self.status = \"processed\"  # 상태 변경\n            return True\n        return False\n\n# 사용\nproducts = [\n    Product(1, \"노트북\", 1000000, 5),\n    Product(2, \"마우스\", 50000, 10)\n]\n\norder = Order(\"customer123\")\norder.add_item(products[0], 2)\norder.add_item(products[1], 1)\ntotal = order.calculate_total()\norder.process()\n```\n\n**OOP의 특징**: \n- 데이터와 메서드가 캡슐화됨\n- 객체의 상태가 변경됨 (가변성)\n- 상속과 다형성으로 확장\n\n### 🔧 함수형 프로그래밍 (FP)\n\n```python\nfrom functools import reduce\nfrom typing import Dict, List, Tuple\n\n# 순수 함수들\ndef reduce_stock(product: Dict, quantity: int) -> Tuple[Dict, bool]:\n    \"\"\"재고 감소 - 새로운 product 반환\"\"\"\n    if product['stock'] >= quantity:\n        new_product = {**product, 'stock': product['stock'] - quantity}\n        return new_product, True\n    return product, False\n\ndef add_item_to_order(order: Dict, product: Dict, quantity: int) -> Dict:\n    \"\"\"주문에 아이템 추가 - 새로운 order 반환\"\"\"\n    updated_product, success = reduce_stock(product, quantity)\n    \n    if success:\n        new_item = {'product_id': product['id'], 'quantity': quantity, 'price': product['price']}\n        return {\n            **order,\n            'items': order['items'] + [new_item],  # 불변성 - 새 리스트 생성\n            'products': {**order['products'], product['id']: updated_product}\n        }\n    return order\n\ndef calculate_total(order: Dict) -> int:\n    \"\"\"주문 총액 계산\"\"\"\n    return reduce(\n        lambda total, item: total + (item['price'] * item['quantity']),\n        order['items'],\n        0\n    )\n\ndef process_order(order: Dict) -> Dict:\n    \"\"\"주문 처리 - 새로운 order 반환\"\"\"\n    if order['items']:\n        return {**order, 'status': 'processed'}\n    return order\n\n# 사용\nproducts = {\n    1: {'id': 1, 'name': '노트북', 'price': 1000000, 'stock': 5},\n    2: {'id': 2, 'name': '마우스', 'price': 50000, 'stock': 10}\n}\n\ninitial_order = {\n    'customer_id': 'customer123',\n    'items': [],\n    'status': 'pending',\n    'products': products\n}\n\n# 함수 체이닝으로 데이터 변환\norder = (initial_order\n         |> lambda o: add_item_to_order(o, products[1], 2)\n         |> lambda o: add_item_to_order(o, products[2], 1)\n         |> process_order)\n\ntotal = calculate_total(order)\n```\n\n**FP의 특징**:\n- 순수 함수로 구성\n- 불변성 - 원본 데이터 보존\n- 함수 합성으로 복잡한 로직 구현\n\n### 📊 데이터 지향 프로그래밍 (DOP)\n\n```python\n# 1. 데이터 우선 설계 - 먼저 데이터 구조 정의\nOrderData = {\n    'customer_id': str,\n    'items': List[Dict],\n    'status': str,\n    'timestamp': str\n}\n\nProductData = {\n    'id': int,\n    'name': str, \n    'price': int,\n    'stock': int\n}\n\n# 2. 데이터와 로직 완전 분리\ndef update_stock(products: List[Dict], product_id: int, quantity: int) -> List[Dict]:\n    \"\"\"재고 업데이트 - 데이터 변환에 집중\"\"\"\n    return [\n        {**p, 'stock': p['stock'] - quantity} \n        if p['id'] == product_id and p['stock'] >= quantity \n        else p\n        for p in products\n    ]\n\ndef add_order_item(order_data: Dict, product_id: int, quantity: int, price: int) -> Dict:\n    \"\"\"주문 아이템 추가 - 순수한 데이터 변환\"\"\"\n    new_item = {\n        'product_id': product_id,\n        'quantity': quantity, \n        'price': price,\n        'total': price * quantity\n    }\n    \n    return {\n        **order_data,\n        'items': [*order_data['items'], new_item]  # 불변성\n    }\n\ndef calculate_order_total(order_data: Dict) -> int:\n    \"\"\"총액 계산 - 데이터에서 값 추출\"\"\"\n    return sum(item['total'] for item in order_data['items'])\n\ndef transform_order_status(order_data: Dict, new_status: str) -> Dict:\n    \"\"\"상태 변환 - 데이터 변환\"\"\"\n    return {**order_data, 'status': new_status}\n\n# 3. 데이터 변환 파이프라인\ndef process_order_pipeline(products_data: List[Dict], order_data: Dict) -> Tuple[List[Dict], Dict]:\n    \"\"\"전체 주문 처리 파이프라인\"\"\"\n    \n    # 단계별 데이터 변환\n    updated_products = products_data\n    updated_order = order_data\n    \n    # 노트북 2개 추가\n    if any(p['id'] == 1 and p['stock'] >= 2 for p in updated_products):\n        updated_products = update_stock(updated_products, 1, 2)\n        updated_order = add_order_item(updated_order, 1, 2, 1000000)\n    \n    # 마우스 1개 추가  \n    if any(p['id'] == 2 and p['stock'] >= 1 for p in updated_products):\n        updated_products = update_stock(updated_products, 2, 1)\n        updated_order = add_order_item(updated_order, 2, 1, 50000)\n    \n    # 상태 변경\n    if updated_order['items']:\n        updated_order = transform_order_status(updated_order, 'processed')\n    \n    return updated_products, updated_order\n\n# 사용 - 단순한 데이터 구조\nproducts_data = [\n    {'id': 1, 'name': '노트북', 'price': 1000000, 'stock': 5},\n    {'id': 2, 'name': '마우스', 'price': 50000, 'stock': 10}\n]\n\norder_data = {\n    'customer_id': 'customer123',\n    'items': [],\n    'status': 'pending',\n    'timestamp': '2024-01-01T00:00:00'\n}\n\n# 파이프라인 실행\nfinal_products, final_order = process_order_pipeline(products_data, order_data)\ntotal = calculate_order_total(final_order)\n```\n\n## 🔍 핵심 차이점 분석\n\n### 1. **데이터 우선 설계**\n- **OOP**: 클래스 설계부터 시작\n- **FP**: 함수 시그니처 설계부터 시작  \n- **DOP**: 데이터 구조 설계부터 시작 ✅\n\n### 2. **불변성 처리**\n- **OOP**: 객체 상태 직접 변경 (`self._stock -= quantity`)\n- **FP**: 새로운 데이터 반환 (`{**product, 'stock': ...}`)\n- **DOP**: 데이터 변환 파이프라인 (`[*items, new_item]`) ✅\n\n### 3. **데이터-로직 분리**\n- **OOP**: 데이터와 메서드가 함께 (`class Product`)\n- **FP**: 함수 중심이지만 여전히 결합\n- **DOP**: 완전한 분리 (`ProductData`와 `update_stock` 함수) ✅\n\n## 💡 실무에서의 선택 기준\n\n태수야, 실제로는 이렇게 선택하면 좋아:\n\n- **OOP**: 복잡한 도메인 모델링, 팀이 객체지향에 익숙할 때\n- **FP**: 데이터 변환이 많고, 동시성이 중요할 때  \n- **DOP**: 대량 데이터 처리, 성능이 중요하고, API 통신이 많을 때\n\n각 패러다임의 장단점을 이해하고 상황에 맞게 섞어서 쓰는 것이 가장 현명한 접근이야. DOP는 특히 현대의 마이크로서비스나 데이터 파이프라인에서 빛을 발하고 있어.",
      "has_relevant_content": true,
      "elapsed_time": 52.36,
      "timestamp": "2025-08-26T08:30:02.739039+00:00",
      "document_paths": [
        "references/OOP.md",
        "references/FP.md",
        "references/DOP.md"
      ],
      "success": true
    }
  }
}