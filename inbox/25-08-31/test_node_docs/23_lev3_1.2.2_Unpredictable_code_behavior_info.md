# 속성
---
content_processing: unified
created_at: 2025-08-28 11:43:31.980838
folder_name: null
process_status: true
source: 2022_Data-Oriented Programming_Manning.pdf
source_language: english
source_type: book
structure_type: component
title: Data-Oriented Programming
---
## 핵심 내용
이 문서는 객체 지향 프로그래밍(OOP)에서 객체의 가변성으로 인해 발생하는 예측 불가능한 코드 동작을 설명합니다. 다중 스레드 환경에서 객체의 멤버 변수가 변경될 수 있어, 같은 코드를 두 번 실행해도 다른 결과를 얻을 수 있습니다. OOP에서는 이러한 문제를 해결하기 위해 동시성 안전 메커니즘을 사용하지만, 이는 성능 저하와 교착 상태의 위험을 초래합니다.

## 상세 핵심 내용
이 문서는 예측 불가능한 코드 동작의 원리를 설명합니다. 첫 번째 코드 예시(Listing 1.1)는 원시 값에 접근하여 두 번 모두 동일한 값을 출력하지만, 두 번째 예시(Listing 1.2)는 객체 멤버에 접근하여 다중 스레드 환경에서 예측 불가능한 결과를 초래할 수 있습니다. 객체 멤버는 변경 가능하기 때문에, 두 console.log 호출 사이에 객체의 상태가 변경될 수 있기 때문입니다. 이러한 예측 불가능성은 OOP의 문제점 중 하나이며, 데이터의 가변성(mutability)이 주요 원인입니다. OOP에서 이러한 문제를 해결하기 위해 mutex와 같은 동시성 안전 메커니즘을 사용할 수 있지만, 성능 저하 및 데드락 위험과 같은 다른 문제가 발생할 수 있습니다. DOP(Data-oriented programming)는 모든 데이터를 불변 값으로 처리하여 개발자에게 예측 가능한 환경을 제공합니다.

## 상세 정보
## 1.2.2 예측 불가능한 코드 동작 (Unpredictable code behavior)

**1. 개요:**

*   이 섹션에서는 코드의 예측 불가능한 동작에 대해 설명하며, 특히 객체 지향 프로그래밍(OOP)의 문제점을 지적한다.
*   단순해 보이는 코드에서도 멀티 스레드 환경이나 비동기 코드 사용 시 예측 불가능한 결과를 초래할 수 있음을 보여준다.

**2. 문제 제기:**

*   **Listing 1.1 (단순 코드):** `isBlocked` 상태를 변수에 할당한 후 두 번 출력한다.
    *   첫 번째 `console.log`에서 `true`가 출력되면, 두 번째 `console.log`에서도 `true`가 출력된다. (예측 가능)
*   **Listing 1.2 (약간 다른 코드 - 객체 멤버 접근):** `isBlocked` 상태를 변수에 할당하지 않고 직접 두 번 출력한다.
    *   첫 번째 `console.log`에서 `true`가 출력되면, 두 번째 `console.log`의 결과는 예측 불가능하다.
        *   **멀티 스레드 환경:** 두 `console.log` 호출 사이에 다른 스레드에 의해 객체의 상태가 변경될 수 있다. (예: 회원의 차단 해제)
        *   **단일 스레드 환경 (JavaScript):** 비동기 코드를 통해 데이터가 변경될 수 있다. (부록 A의 원칙 #3 참조)

**3. 원인 분석:**

*   **Listing 1.1 vs Listing 1.2의 차이:**
    *   **Listing 1.1:** 원시 타입(primitive type)의 값을 두 번 접근한다. 원시 타입은 일반적으로 불변(immutable)하다.
    *   **Listing 1.2:** 객체의 멤버를 두 번 접근한다. 객체 멤버는 가변(mutable)할 수 있다.
*   **핵심:** 가변(mutable) 데이터는 코드의 예측 불가능성을 유발한다.

**4. OOP의 문제점:**

*   OOP는 객체 멤버의 가변성 때문에 예측 불가능한 동작을 야기할 수 있다.
*   문제 해결을 위해 동시성 안전 메커니즘 (예: 뮤텍스)을 사용할 수 있지만, 성능 저하 및 데드락(deadlock) 발생의 위험이 있다.

**5. DOP (Data-Oriented Programming)의 해결책:**

*   DOP는 모든 데이터를 동일하게 취급한다.
*   원시 타입과 컬렉션 타입을 포함한 모든 데이터를 불변(immutable) 값으로 취급한다.
*   **결과:** DOP 개발자는 데이터의 불변성으로 인해 마음의 평화를 얻고, 애플리케이션 개발에 더 집중할 수 있다.

**6. 핵심 요약 (TIP):**

*   **가변 데이터는 코드의 예측 불가능성을 유발한다.**
*   **데이터 불변성은 DOP 개발자의 마음에 평화를 가져다준다.**

**7. 문서 정보:**

*   **페이지:** 46
*   **챕터:** 1 (객체 지향 프로그래밍의 복잡성)
*   **Listing 1.1:** Really simple code
*   **Listing 1.2:** Apparently simple code

## 주요 화제
문서의 핵심 주제는 다음과 같습니다.

*   **예측 불가능한 코드 동작:** 객체 지향 프로그래밍(OOP)에서 발생하는 예측 불가능한 코드 동작에 대한 설명.
*   **코드 예시:** `displayBlockedStatusTwice` 함수를 통해 보여주는 예측 불가능한 동작 예시 (싱글 스레드 및 멀티 스레드 환경에서의 차이).
*   **가변성(Mutability)과 예측 불가능성:** 데이터의 가변성이 코드의 예측 불가능성을 유발한다는 점을 강조.
*   **OOP의 문제점:** 객체 멤버의 가변성으로 인한 문제와 이를 해결하기 위한 방법 (mutex 등) 제시.
*   **DOP (Data-Oriented Programming)의 해결책:** DOP가 모든 데이터를 불변 값으로 처리하여 예측 가능성을 높이고 개발 생산성을 향상시키는 방식 설명.
*   **결론:** 데이터 불변성이 DOP 개발자에게 안정감을 준다는 점을 강조.

## 부차 화제
다음은 주어진 문서에서 주요 주제 외에 언급된 부차적인 주제들을 불렛 포인트로 정리한 것입니다.

*   **단일 스레드 및 멀티 스레드 환경의 차이:** 코드 동작의 예측 가능성에 미치는 영향
*   **비동기 코드의 영향:** 자바스크립트와 같은 단일 스레드 환경에서도 예측 불가능성이 발생할 수 있음
*   **OOP의 문제점:** 객체 지향 프로그래밍(OOP)에서 가변성으로 인한 예측 불가능한 동작
*   **가변 데이터의 위험성:** 데이터가 가변적일 때 코드의 예측 불가능성이 증가
*   **OOP에서의 해결책:** 동시성 안전 메커니즘(예: 뮤텍스)을 사용한 민감한 코드 보호
*   **OOP 해결책의 단점:** 성능 저하 및 교착 상태 발생 위험
*   **DOP (Data-Oriented Programming)의 특징:** 모든 데이터를 불변 값으로 취급하여 개발자의 안정성을 높임
*   **데이터 불변성의 장점:** DOP 개발자에게 안정성을 제공
*   **원시 타입과 객체 멤버의 차이:** 동작 예측 가능성에 미치는 영향
*   **코드 예시 (Listing 1.1 및 Listing 1.2):** 코드의 차이점을 비교 분석하여 예측 불가능성의 원리를 설명

# 내용
---
### 1.2.2 Unpredictable code behavior

1.2.2
Unpredictable code behavior
You might be a bit tired after the system-level analysis that we presented in the previ-
ous section. Let's get refreshed and look at some code.
 Take a look at the code in listing 1.1, where we get the blocked status of a member
and display it twice. If I tell you that when I called displayBlockedStatusTwice, the
program displayed true on the first console.log call, can you tell me what the pro-
gram displayed on the second console.log call?

class Member {
  isBlocked;
  displayBlockedStatusTwice() {
    var isBlocked = this.isBlocked;
    console.log(isBlocked);
    console.log(isBlocked);
  }
}
member.displayBlockedStatusTwice();

"Of course, it displayed true again," you say. And you are right!
 Now, take a look at a slightly different pseudocode as shown in listing 1.2. Here we
display, twice, the blocked status of a member without assigning a variable. Same ques-
tion as before: if I tell you that when I called displayBlockedStatusTwice, the pro-
gram displayed true on the first console.log call, can you tell me what the program
displayed on the second console.log call?

class Member {
  isBlocked;
  displayBlockedStatusTwice() {
    console.log(this.isBlocked);
    console.log(this.isBlocked);
  }
}
member.displayBlockedStatusTwice();

The correct answer is . . . in a single-threaded environment, it displays true, while in a
multi-threaded environment, it's unpredictable. Indeed, in a multi-threaded environ-
ment between the two console.log calls, there could be a context switch that changes
the state of the object (e.g., a librarian unblocked the member). In fact, with a slight
modification, the same kind of code unpredictability could occur even in a single-
threaded environment like JavaScript, when data is modified via asynchronous code
(see the section about Principle #3 in appendix A). The difference between the two
code snippets is that
In the first listing (listing 1.1), we access a Boolean value twice , which is a prim-
itive value.
In the second listing (listing 1.2), we access a member of an object twice.

TIP
When data is mutable, code is unpredictable.

Listing 1.1
Really simple code

Listing 1.2
Apparently simple code

--- 페이지 46 ---
18
CHAPTER 1
Complexity of object-oriented programming
This unpredictable behavior of the second listing is one of the annoying conse-
quences of OOP. Unlike primitive types, which are usually immutable, object mem-
bers are mutable. One way to solve this problem in OOP is to protect sensitive code
with concurrency safety mechanisms like mutexes, but that introduces issues like a
performance hit and a risk of deadlocks.
 We will see later in the book that DOP treats every piece of data in the same way:
both primitive types and collection types are immutable values. This value treatment for
all citizens brings serenity to DOP developers' minds, and more brain cells are avail-
able to handle the interesting pieces of the applications they build.

TIP
Data immutability brings serenity to DOP developers' minds.

# 구성
---

